// Vercel entry point — hands every /api request to the Express app in app.js.
// (On cPanel or a normal server you don't use this file; you run app.js directly.)
module.exports = require("/app.js");
